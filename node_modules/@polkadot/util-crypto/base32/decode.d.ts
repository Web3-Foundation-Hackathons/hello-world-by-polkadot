/**
 * @name base32Decode
 * @summary Delookup a base32 value.
 * @description
 * From the provided input, decode the base32 and return the result as an `Uint8Array`.
 */
export default function base32Decode(value: string, ipfsCompat?: boolean): Uint8Array;
