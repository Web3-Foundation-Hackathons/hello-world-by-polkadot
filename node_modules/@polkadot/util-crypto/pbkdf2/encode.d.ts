interface Result {
    password: Uint8Array;
    rounds: number;
    salt: Uint8Array;
}
export default function pbkdf2Encode(passphrase?: Uint8Array | string, salt?: Uint8Array, rounds?: number): Result;
export {};
