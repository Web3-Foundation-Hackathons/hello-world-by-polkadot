import { Params } from './types';
interface Result {
    params: Params;
    password: Uint8Array;
    salt: Uint8Array;
}
export default function scryptEncode(passphrase?: Uint8Array | string, salt?: Uint8Array, params?: {
    N: number;
    p: number;
    r: number;
}): Result;
export {};
