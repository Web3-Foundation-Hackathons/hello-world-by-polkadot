"use strict";

// Copyright 2017-2020 @polkadot/util authors & contributors
// SPDX-License-Identifier: Apache-2.0

/* eslint-disable @typescript-eslint/unbound-method */
describe('String padEnd', () => {
  let stringEnd;
  beforeEach(() => {
    stringEnd = String.prototype.padEnd; // eslint-disable-next-line no-extend-native,@typescript-eslint/no-explicit-any,@typescript-eslint/no-unsafe-assignment

    String.prototype.padEnd = null;

    require('./padEnd');
  });
  afterEach(() => {
    // eslint-disable-next-line no-extend-native
    String.prototype.padEnd = stringEnd;
  });
  it('does padding', () => {
    expect('test'.padEnd(8, 'A')).toEqual('testAAAA');
    expect('test'.padEnd(8)).toEqual('test    ');
  });
});