import { TextEncoder as NodeTextEncoder } from 'util';
declare const _default: {
    new (): TextEncoder;
    prototype: TextEncoder;
} | {
    new (): {
        "__#1@#encoder": NodeTextEncoder;
        encode(value: string): Uint8Array;
    };
};
export default _default;
